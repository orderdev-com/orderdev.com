{
  "name": "orderdev.com-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "turbo run dev",
    "db-generate": "turbo run db-generate",
    "db-migrate": "turbo run db-migrate",
    "build": "turbo run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "turbo": "^2.0.6",
    "wait-on": "^7.2.0"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
