{
	"name": "db-drizzle",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "run-s wait-turso-dev-port && pnpm generate && tsx src/migrate.ts",
		"wait-turso-dev-port": "wait-on tcp:8022",
		"generate": "drizzle-kit generate",
		"migrate": "drizzle-kit migrate"
	},
	"dependencies": {
		"@libsql/client": "^0.7.0",
		"dotenv": "^16.4.5",
		"drizzle-orm": "^0.31.2"
	},
	"devDependencies": {
		"drizzle-kit": "^0.22.8",
		"tsx": "^4.7.1"
	}
}
